import Placeholder from "../components/Placeholder";

export default function NotFound() {
  return (
    <Placeholder
      title="Page not found"
      description="That route does not exist. If you followed a link from inside the app, that is a bug worth reporting."
    />
  );
}
