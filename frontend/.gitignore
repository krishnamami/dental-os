# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Infrastructure outputs — environment, not source.
infra.env
.infra/

# Local env overrides. .env.production and .env.example ARE tracked —
# VITE_ vars are inlined into the public bundle, so none of them are
# secret and a committed default keeps CI builds reproducible.
.env.local
.env.*.local
