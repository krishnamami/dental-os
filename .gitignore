.env
__pycache__/
.venv/
*.pyc
.DS_Store
*.egg-info/

# local scratch
_chk.py
_probe_tmp.cjs

# Playwright was installed at the repo root; the Node project belongs in
# frontend/. Anchored with a leading slash so frontend/package.json — which
# IS tracked — keeps working.
/node_modules/
/package.json
/package-lock.json

# Recorder output — a 3MB+ binary per take, and the raw WebM scratch dir.
/accord_dental_demo.mp4
/accord_dental_FINAL.mp4
/accord_dental_voiceover.mp3
/.video_tmp/
